SELECT BS_ID,
	    PHARMACY_ID,
	    ID_TBL,
	    ACT_TYP,
	    MAX (ID_LOG) ID_LOG
FROM ITEMS_DETAILS
WHERE NVL (IND_ORSIM, 'N') = 'N'
AND
	TRUNC (TS_REG) >= TRUNC (SYSDATE - 7)
GROUP BY
	BS_ID,
	PHARMACY_ID,
	ID_TBL,
	ACT_TYP
ORDER BY
	3 ASC;